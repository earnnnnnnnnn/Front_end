<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Camera Details</title>
</head>
<body>
    <div class="camera-detail">
        <h1><%= camera.cameraname %></h1>
        <img src="../image/<%= camera.cameraimg %>" alt="<%= camera.cameraname %>">
        <p><strong>Brand:</strong> <%= camera.brand %></p>
        <p><strong>Status:</strong> <%= camera.status %></p>
        <p><strong>Price per day:</strong> <%= camera.rental_price_per_day %> Baht</p>
        <p><strong>Replacement cost:</strong> <%= camera.replacement_cost %> Baht</p>
    </div>
    <a href="/">Back to Home</a>
</body>
</html>
