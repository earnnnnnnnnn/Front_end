<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Books</h1>
    <table>
        <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Actions</th>
        </tr>
        <% for(var i=0; i< users.length; i++) { %>
        <tr>
            <td><%= users[i].username %></td>
            <td><%= users[i].email %></td>
            <!-- <td>
                <a href="/book/<%= users[i].id %>">View</a>
                <a href="/update/<%= users[i].id %>">Edit</a>
                <a href="/delete/<%= users[i].id %>">Delete</a>
            </td> -->
        </tr>
        <% } %>
    </table>
    <a href="/create">Create New Book</a>
</body>
</html>