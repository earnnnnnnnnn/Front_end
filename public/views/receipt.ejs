<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Receipt</title>
  <link rel="stylesheet" href="../css/receipt.css">
  
</head>

<body>

  <div class="container">
    <h1>Receipt</h1>

    <div class="section">
      <label>Order ID:</label> <span>
        <%= payment.payment_id %>
      </span>
    </div>

    <div class="section">
      <label>User ID:</label> <span>
        <%= user.users_id %>
      </span>
    </div>

    <div class="section">
      <label>Username:</label> <span>
        <%= user.username %>
      </span>
    </div>

    <div class="section">
      <label>Email:</label> <span>
        <%= user.email %>
      </span>
    </div>

    <h3>Date:</h3>
    <% const startDateThai=new Date(payment.startDate).toLocaleDateString('th-TH', { day: '2-digit' , month: '2-digit' ,
      year: 'numeric' }); const endDateThai=new Date(payment.endDate).toLocaleDateString('th-TH', { day: '2-digit' ,
      month: '2-digit' , year: 'numeric' }); %>

      <div class="section">
        <label>Start Date:</label> <span>
          <%= startDateThai %>
        </span>
      </div>

      <div class="section">
        <label>End Date:</label> <span>
          <%= endDateThai %>
        </span>
      </div>

      <h3>Details:</h3>

      <div class="section">
        <label>Total Price:</label> <span>
          <%= payment.totalPrice %> บาท
        </span>
      </div>

      <div class="section">
        <label>Camera:</label> 

        <% order.forEach(order => { %>
          <% camera.forEach(item => { %>
            <% if (order.camera_id == item.camera_id) { %>
              <span><%= item.cameraname %></span>
            <% } %>
          <% }) %>
        <% }) %>
      </div>

  </div>

</body>

</html>